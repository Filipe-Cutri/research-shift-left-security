# App de teste: OWASP Juice Shop — aplicação intencionalmente vulnerável,
# com vulnerabilidades catalogadas e documentadas (https://owasp-juice.shop)
#
# Para este estudo, o mais simples é usar a imagem oficial como base e
# adicionar aqui apenas o necessário para o pipeline (ex.: healthcheck).
# Alternativa: fazer fork do repo oficial (github.com/juice-shop/juice-shop)
# e usar como submódulo em vez de partir da imagem pronta — dá mais controle
# para "congelar" uma versão específica e documentar exatamente quais
# vulnerabilidades ela contém.

FROM bkimminich/juice-shop:latest

EXPOSE 3000
